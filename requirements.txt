# Python tooling for the MSU-IIT campus Wi-Fi thesis (Week 4 of the roadmap).
# ns-3 itself needs no Python packages; these drive the simulator and visualise its output.
# Install inside WSL:
#   python3 -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt

pandas>=2.2,<3.0
numpy>=1.26,<3.0
altair>=5.4,<6.0
streamlit>=1.40,<2.0
matplotlib>=3.9,<4.0
